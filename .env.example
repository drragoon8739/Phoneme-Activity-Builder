# Copy to .env and adjust if needed.
#
# SQLite path is relative to the prisma/ directory, so this resolves to
# prisma/dev.db when running locally. Docker overrides it to point at the
# mounted volume instead.
DATABASE_URL="file:./dev.db"
